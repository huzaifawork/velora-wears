{
  "name": "velora-wears",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Velora Wears - e-commerce clothing store. One React + Vite application: the storefront and the admin dashboard (admin/src, mounted at /admin), over one Supabase Postgres database.",
  "workspaces": [
    "storefront",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace storefront",
    "build": "npm run build --workspace storefront",
    "preview": "npm run preview --workspace storefront",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspace storefront",
    "db:push": "supabase db push",
    "db:types": "supabase gen types typescript --linked > shared/database.types.ts",
    "functions:deploy": "supabase functions deploy place-order",
    "supabase:link": "supabase link"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
