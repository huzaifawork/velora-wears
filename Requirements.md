```
# Velora Wears E-Commerce Store Requirements
```

# `## 1. Brand Overview` 

```
The brand name is *Velora Wears*.
```

```
The website should be designed as a modern and visually appealing fashion e-
commerce store, similar to the style and shopping experience commonly seen in
Instagram-based clothing brands.
```

```
The overall design should feel premium, modern, stylish, and suitable for a
fashion and clothing brand.
```

```
A suitable logo for *Velora Wears* should also be created and used throughout
the website.
```

```
---
```

# `## 2. Landing Page` 

```
The website should include a professionally designed landing page.
```

```
The landing page should include:
```

- `A visually attractive hero section.` 

- `A brief introduction explaining what *Velora Wears* is and what the brand offers.` 

- `A custom logo created for the brand.` 

- `Featured banners or promotional images.` 

- `A Featured Products section displaying selected products.` 

- `A Categories section, such as Shirts, Hoodies, and other clothing categories.` 

- `Customer testimonials and reviews, preferably presented as feedback from Pakistani customers.` 

- `Attractive call-to-action sections encouraging users to explore products and start shopping.` 

- `A modern footer containing relevant information and useful links.` 

```
The landing page should be visually engaging and optimized to encourage visitors
to browse and purchase products.
```

```
---
```

```
## 3. Products Page
```

```
A dedicated Products page should display all available products.
```

```
Products may include categories such as:
```

- `Shirts` 

- `Hoodies` 

- `Other clothing items` 

```
The page should display products in an organized and visually appealing layout.
```

```
Each product card should include relevant information such as:
```

- `Product image` 

- `Product name` 

- `Price` 

- `Category` 

```
Users should be able to browse and select products easily.
```

```
---
```

```
## 4. Product Details Page
```

```
When a user clicks on a product, such as a shirt or hoodie, they should be taken
to a detailed product page.
```

```
The product details page should include:
```

```
* Product name
```

```
* Product price
```

- `Detailed product description` 

```
* Product category
```

```
* Product images
```

- `Multiple product images, if available` 

- `Image gallery or thumbnail section for viewing different product images * Available sizes` 

```
Available sizes may include:
```

```
* Small
* Medium
* Large
```

```
The user should be able to select the required size before adding the product to
the cart.
```

```
---
```

```
## 5. Categories
```

```
Products should be organized into appropriate categories.
```

```
For example:
```

```
* Shirts
```

```
* Hoodies
```

```
* Other clothing categories
```

```
Users should be able to view and browse products based on their selected
category.
```

```
---
```

```
## 6. Shopping Cart
```

```
Each product should have an *Add to Cart* option.
```

```
Before adding a product to the cart, the user should be able to select the
required size, such as:
```

```
* Small
```

```
* Medium
```

```
* Large
```

```
Once the product is added to the cart, the user should be able to proceed
directly to checkout.
```

```
The shopping cart should clearly display:
```

- `Selected products` 

- `Selected size` 

- `Product quantity` 

- `Product price` 

- `Total order amount` 

```
Users should also be able to update the quantity or remove products from the
cart before checkout.
```

```
---
```

# `## 7. Checkout Process` 

```
Users should be able to place an order without mandatory authentication.
```

```
Both logged-in users and guest users should be able to proceed with checkout.
```

```
Authentication should not be required in order to place an order.
```

```
During checkout, the user should be required to fill in the following
information:
```

```
* Email address
```

- `Full name` 

- `Phone number` 

- `Complete address` 

```
* City
```

```
* Postal code
```

```
After filling in the required information, the user should be able to confirm
and place the order.
```

```
Once the order is successfully confirmed, an order confirmation message or
success page should be displayed to the customer.
```

```
The confirmed order should then be saved and made available in the Admin
Dashboard.
```

```
---
```

```
## 8. Admin Dashboard
```

```
The Admin Dashboard requirements will be provided separately.
```

```
For now, the main requirement is that every confirmed customer order should be
stored and visible in the Admin Dashboard for order management.
```

```
## 9. Payment Method
```

```
For the initial version of the website, the only available payment method will
be:
```

# `* Cash on Delivery (COD)` 

```
Users should be able to place their orders using Cash on Delivery without
requiring any online payment integration.
```

```
Online payment methods may be added in the future if required.
```

```
---
```

# `## 10. Delivery Charges` 

```
Delivery charges should be properly displayed during the checkout process and
included in the final order total.
```

```
For now, the exact delivery charge configuration does not need to be finalized
on the frontend.
```

```
The delivery charges should be manageable from the Admin Dashboard, allowing the
admin to configure and update them as required.
```

```
The configured delivery charges should automatically appear during checkout and
be properly included in the customer's total order amount.
```

```
---
```

```
## 11. Product Quantity, Stock, and Availability
```

```
Each product should support inventory and stock management.
```

```
The website should properly display product availability and stock status.
```

```
This may include:
```

- `Available/In Stock status` 

- `Out of Stock status` 

- `Low Stock status, if required` 

- `Available product quantity` 

```
Appropriate badges should be displayed where necessary, such as:
```

- `In Stock` 

- `Low Stock` 

- `Out of Stock` 

```
If a product or a specific size is out of stock, the user should be clearly
informed and should not be able to purchase that unavailable option.
```

```
Stock availability should also work correctly based on different product sizes
where applicable.
```

```
---
```

```
## 12. Order Confirmation Animation
```

```
After the customer successfully confirms and places an order, they should be
redirected to an attractive Order Success page.
```

```
The success page should include an engaging animated order confirmation
experience.
```

```
For example, the animation may show:
```

- `A package being prepared and packed` 

- `A delivery truck arriving` 

- `The order/package being loaded into the truck` 

- `A successful order confirmation message` 

```
The overall experience should feel modern, visually appealing, and engaging
rather than simply displaying a basic confirmation message.
```

```
The page should clearly inform the customer that their order has been
successfully placed.
```

```
---
```

# `## 13. Product Search` 

```
The website should include a product search feature.
```

```
The search should not automatically execute on every character change.
```

```
Instead, users should be able to:
```

- `Type their search query` 

- `Press the Enter key to perform the search, or` 

- `Click a Search button` 

```
The system should then display products matching the entered search query.
```

```
---
```

```
## 14. Product Filters and Sorting
```

```
The Products page should include a complete and user-friendly filtering and
sorting system.
```

```
Users should be able to filter products based on categories, such as:
```

- `Shirts` 

- `Hoodies` 

- `Other available categories` 

```
The filtering system should be properly designed and easy to use.
```

```
Users should also be able to sort products based on pricing and other useful
options.
```

```
At a minimum, the following sorting options should be included:
```

- `Price: Low to High` 

- `Price: High to Low` 

```
Additional useful filtering and sorting options may also be included where
appropriate to provide a better shopping experience.
```

```
---
```

```
## 15. Mobile Responsiveness
```

```
The entire website must be fully responsive and optimized for different screen
sizes.
```

```
The website should work properly on:
```

- `Mobile phones` 

- `Tablets` 

- `Laptops` 

- `Desktop devices` 

```
Special attention should be given to the mobile shopping experience, including:
```

- `Mobile-friendly navigation` 

- `Responsive product grids` 

- `Easy-to-use filters and search` 

- `Properly optimized product pages` 

- `A smooth cart and checkout experience` 

- `Responsive order confirmation animation` 

```
The final website should provide a smooth and professional user experience
across all devices.
```


---

## 16. Customer Reviews and Ratings

The website should allow customers to leave reviews on products they have actually
purchased.

### Who can leave a review

Reviews are tied to a **confirmed purchase**, not to having an account. Two paths must
both be supported:

- **Signed-in customers.** A user who signs in and has bought a product should be able
  to leave a review for that product.
- **Guest customers.** A guest who checked out without signing in and whose order was
  successfully confirmed should also be able to leave a review for the product they
  bought. Not having an account must not block them from reviewing.

A visitor who has not purchased a given product should not be able to review it.

### How a guest review is authorised

Because guests have no account, review access should be granted through the order itself:

- After a successful order, the Order Success page should offer a direct link or option to
  review the purchased product(s).
- The review link should be tied to the order reference, so only the person who placed that
  order can use it.
- Where a review is opened later, the guest should be able to verify with their order
  number together with the email address used on the order.

### Review content and rules

- A review should include a star rating and a written comment.
- The reviewer's display name should be shown; the email address must never be shown
  publicly.
- One review per product per order.
- Reviews should be editable or removable by the customer within a reasonable window.
- Empty or whitespace-only reviews must be rejected (see section 17).

### Where reviews appear

- On the Product Details page, with an average rating and the individual reviews.
- The average rating and review count may also appear on product cards.
- Genuine customer reviews may feed the testimonials section on the landing page
  described in section 2.

### Admin

- All reviews should be visible in the Admin Dashboard.
- The admin should be able to hide or remove a review that is abusive or spam.

---

## 17. Form Validation, Required Fields, and Security

Proper validation and security checks must be applied across the site, and especially at
checkout. The order must not be confirmable while required information is missing or
invalid.

### Required vs optional checkout fields

> **Note:** this list is authoritative and clarifies the field list in section 7 —
> specifically, postal code is **optional**, not required.

| Field | Status | Rule |
| --- | --- | --- |
| Full name | **Required** | Letters and common name characters; minimum sensible length; not blank |
| Email address | **Required** | Must be a valid, correctly formatted email address |
| Phone number | **Required** | Must be a valid Pakistani mobile number (for example `03XXXXXXXXX` or `+92XXXXXXXXXX`) |
| Complete address | **Required** | Not blank; long enough to be a usable delivery address |
| City | **Required** | Needed for delivery and delivery-charge calculation |
| Postal code | *Optional* | Validated for format only if the customer fills it in |
| Order notes / landmark | *Optional* | Free text |

### Validation behaviour

- The customer must not be able to move past checkout, and **the order must not be
  confirmed**, while any required field is blank or invalid.
- Invalid or missing fields should be clearly marked with a helpful message explaining what
  is wrong.
- Validation should run both as the customer fills the form and again when they submit.
- Whitespace-only input must be treated as blank.
- The same validation rules apply to guest checkout and signed-in checkout.

### Server-side checks

Client-side validation is for user experience only and must never be the only defence.
Every rule above must be enforced again on the server before an order is saved:

- Re-validate every required field on the server; reject the order if anything fails.
- **Never trust prices, totals, or delivery charges sent from the browser.** Recalculate
  the order total on the server from stored product prices and the admin-configured
  delivery charge (sections 6 and 10).
- Re-check stock and size availability at the moment of confirmation, so an item that sold
  out during checkout cannot be ordered (section 11).
- Reject malformed, oversized, or unexpected input.

### General security

- Sanitise and escape all customer-supplied text — including names, addresses, and review
  comments — before storing or displaying it.
- Apply rate limiting to order placement, review submission, and search to prevent abuse
  and spam.
- Database rules must stay closed to direct client access; all writes go through trusted
  server-side code.
- Customer personal data (email, phone, address) must never be publicly readable, and must
  not be exposed through reviews or any public page.
- Sign-in must use secure, standard authentication with proper session handling.

---

## 18. Technical Stack and Architecture

### Frontend

The storefront must be built with **React + Vite** (TypeScript). This is a firm
requirement, not a preference.

### Database

The project uses the **Firebase Realtime Database**.

> **Clarification:** Firestore was mentioned informally in discussion, but the project is
> confirmed on Realtime Database — that is the instance that is enabled, configured, and
> live (`velora-wears-default-rtdb`, region `asia-southeast1`). Both the storefront and the
> admin dashboard read and write the **same** Realtime Database. Any proposal to move to
> Firestore must be agreed by both developers, because it changes every query on both sides.

### Why there is server-side code

The storefront is a browser-only single-page application, so it **cannot** hold the Firebase
Admin SDK service account key — shipping that key to the browser would give any visitor full
control of the database. Therefore:

- The browser reads the public catalog directly using the Firebase **client** SDK, which is
  fast and cacheable.
- Anything involving money, stock, or customer data — placing an order, submitting a review
  — goes through **Cloud Functions**, which run trusted server-side code. This is what makes
  the server-side validation in section 17 possible.
- Database rules deny all direct client writes.

### Repository layout

The storefront and the admin dashboard live in the **same repository**, in separate folders,
sharing one documented data contract.

```
storefront/   React + Vite storefront
admin/        Admin dashboard (second developer)
functions/    Cloud Functions - trusted server-side code
shared/       Shared TypeScript types - the data contract
```

### Component reuse — no duplicated code

The UI must be built from **reusable components**. Writing the same markup or logic again in
a different file is not acceptable.

- Shared UI elements — buttons, inputs, product cards, badges, modals, ratings — must each
  exist **once**, in a shared component, and be reused with props everywhere they appear.
- Layout concerns such as page width and gutters live in one layout component, not repeated
  per page.
- Colours, spacing, and typography come from shared design tokens. Components must not
  hardcode raw colour values.
- Repeated data-fetching or formatting logic belongs in a shared hook or utility, not copied
  between pages.
- Before writing a new component, check whether an existing one can take a prop instead.

### Data source — a temporary decision, not part of the spec

> **Status: temporary. Added 2026-08-28. Remove this subsection when it no longer applies.**

The client has not purchased the Firebase Blaze plan yet. Until the storefront has been
reviewed and Blaze is enabled, the catalog is served from **demo data held in the frontend**
rather than from the Realtime Database.

This is a staging decision only. It does **not** change the architecture above, and it is
bound by two rules that keep it from becoming technical debt:

1. **The data layer's public interface does not change.** Demo data is served through the
   same `queries.ts` functions, with the same async signatures, typed against the same
   `shared/types.ts` contract. Pages and components import from `queries.ts` and must never
   import the demo data directly — so they cannot tell the difference, and none of them need
   editing when the switch happens.
2. **Switching to the Realtime Database is a one-file change**, controlled by a single flag.
   The Realtime Database implementation stays in place and is not deleted.

Demo product images are committed to the repository and served as static assets. They are
not uploaded to Firebase Storage, which is unavailable without Blaze.

Both the demo data and the demo images are **throwaway**. They are removed once the admin
dashboard (section 8) can create real products, and the storefront then reads live data as
described in sections 18 and 19.

---

## 19. Performance and Query Optimisation

The website must feel **fast**. Both developers are responsible for this, because the
storefront's speed depends directly on how the admin dashboard writes data.

### Query optimisation

- **Every query must be indexed.** Any field used for ordering or filtering needs a matching
  `.indexOn` entry in the database rules. Without it, Firebase downloads the entire node and
  sorts it in the browser — the most common cause of a slow Realtime Database app.
- **Never read an unbounded list.** Every query must be limited and paginated.
- **Separate list data from detail data.** List views (products grid, search results,
  category pages) must read a small denormalised summary record, not the full product. Full
  descriptions and full-size image references are fetched only when a customer opens a
  single product.
- **Denormalise for reads.** The Realtime Database cannot filter and sort on two different
  fields in one query. Where a combined filter and sort is needed, store a precomputed field
  for it rather than downloading extra data and filtering in the browser.
- **Precompute derived values at write time** — stock status, average rating, review count,
  search text — so the storefront never has to compute them across many records.
- **Cache reads on the client** so moving between pages does not refetch unchanged data.

### Image optimisation

Images are the largest thing on a clothing site and matter more than anything else here.

- Each product image must be stored in **at least two variants**: a small `thumb` for cards
  and grids, and a `full` version for the product detail gallery. Product listings must
  never load full-resolution images.
- Images must be compressed, and served in a modern format (such as WebP) where possible.
- Images below the fold must be **lazy loaded**; the hero and above-the-fold images should
  load eagerly.
- Image dimensions must be known so space is reserved and the page does not shift while
  loading.
- The product detail gallery should load the first image immediately and the remaining
  thumbnails afterwards.

### Application performance

- Route-level code splitting, so the first page load does not download the whole app.
- Vendor code split from application code so cached bundles survive updates.
- Debounced or explicit-trigger search (section 13 already requires search on Enter or
  button, not per keystroke).
- Skeleton or loading states instead of blank screens.

---

## 20. Team, Ownership, and Responsibilities

Two developers work on this project, sharing one repository and one database.

| Area | Owner |
| --- | --- |
| Storefront (React + Vite) — sections 1-7, 9-19 | Developer A |
| **Admin Dashboard — section 8** | **Developer B (Huzaifa's friend)** |
| Cloud Functions / order placement | Developer A |
| Database rules and indexes | Both, agreed jointly |
| `shared/types.ts` data contract | Both, agreed jointly |

**The Admin Dashboard will be built by the second developer.** The storefront developer is
not responsible for building it. The detailed admin dashboard specification is still to be
provided (section 8).

### The shared contract

Because the two applications share one database, the boundary between them must be explicit:

- `shared/types.ts` is the **single source of truth** for the shape of all stored data.
  Neither developer may change a stored field unilaterally — a change there is a breaking
  change for the other side.
- Whoever writes a new query must add the matching `.indexOn` entry to the database rules in
  the same change.
- **The admin dashboard must keep the denormalised summary records in sync** whenever a
  product is created or edited. The storefront's listing, search, and category pages read
  those summaries, so a stale summary shows customers wrong prices or wrong stock.
- **The admin dashboard must write both image variants** (`thumb` and `full`) when uploading
  product images, per section 19.
- Admin write access is granted through Firebase Auth: the admin's user ID must be listed
  under `admins/{uid}` in the database.
